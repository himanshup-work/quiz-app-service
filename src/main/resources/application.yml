spring.profiles.active: local

quiz.app.service:
  dataSource:
    driverClassName: driver-class-name
    url: db-url
    username: db-username
    password: db-username
    schema: application-schema-name

logging.level.org.springframework.boot.context.properties: DEBUG